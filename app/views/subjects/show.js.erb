console.log("asd");

console.log("#lesson-" +<%= @current_lesson.id %>);

$("#lesson-" +<%= @current_lesson.id %>).siblings().removeClass("active");
$("#lesson-" +<%= @current_lesson.id %>).addClass("active");

$('.video').fadeOut("fast", function(){

	$(this).html("<%= j render partial: 'lessons/lesson_video', locals: {lesson: @current_lesson} %>");
	$(this).fadeIn();

});

$('.lesson-description').fadeOut("fast", function(){
	$(this).html("<%= j render partial: 'lessons/lesson_description', locals: {lesson: @current_lesson} %>");
	$(this).fadeIn();
});