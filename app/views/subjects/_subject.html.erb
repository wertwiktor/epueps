<article class="subject">
	<!-- TODO: Add link to subject -->
	<!-- <h2><%= link_to subject.name, subject_path(subject) %></h2> -->
	<h2><%= subject.name %></h2>
	<%= image_tag subject.image_src %>
	<section class = "description">
		<!-- TODO: Move this to subject_description helper -->
		<%= truncate 	sanitize(subject.description), 
									length: 200,
									separatior: ' '  %>
	</section>	

	<%= link_to "Zacznij kurs", subject, class: "button button-primary" %>
</article>
