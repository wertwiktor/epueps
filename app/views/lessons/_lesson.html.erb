<li class = "lesson" id = <%= "lesson-#{lesson.id}" %>>
	<%= link_to lesson.name, 
										subject_path(	lesson.subject, 
																	params: {lesson_id: lesson.id}) %>
	
	<%= image_tag current_lesson_video.thumbnail %>
</li>